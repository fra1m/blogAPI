extends ../layouts/app.pug

block content
  div
    a(href='/') Main page
  table.table
    tbody
      tr
        th Title
      each post in posts
        tr
          td
            a(href=`/posts/${post.id}`) #{post.title}

